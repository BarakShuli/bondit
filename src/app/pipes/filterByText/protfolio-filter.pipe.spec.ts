import { ProtfolioFilterPipe } from './protfolio-filter.pipe';

describe('ProtfolioFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProtfolioFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
