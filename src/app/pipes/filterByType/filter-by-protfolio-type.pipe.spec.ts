import { FilterByProtfolioTypePipe } from './filter-by-protfolio-type.pipe';

describe('FilterByProtfolioTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByProtfolioTypePipe();
    expect(pipe).toBeTruthy();
  });
});
