import { GetUniqeTypeListPipe } from './get-uniqe-type-list.pipe';

describe('GetUniqeTypeListPipe', () => {
  it('create an instance', () => {
    const pipe = new GetUniqeTypeListPipe();
    expect(pipe).toBeTruthy();
  });
});
